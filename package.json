{
  "name": "recipes",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:ghpages": "vite build --mode ghpages",
    "preview": "vite preview",
    "preview:ghpages": "npm run build:ghpages && vite preview --base /recipes/",
    "deploy:ghpages": "npm run build:ghpages && npx gh-pages -d dist"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.1",
    "gh-pages": "^6.3.0",
    "playwright": "^1.57.0",
    "svelte": "^5.43.8",
    "vite": "^7.2.4"
  }
}
